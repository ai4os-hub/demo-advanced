[tox]
minversion = 3.25.0
isolated_build = True
envlist = qc.sty,qc.cov,qc.sec,py310,py39,py38

[testenv]
install_command = pip install -U {opts} {packages}
commands = pytest --numprocesses=auto --dist=loadscope {posargs}
setenv =
   OS_TEST_TIMEOUT=240
deps = -r{toxinidir}/requirements-test.txt

[testenv:qc.cov]
commands = pytest --cov=api --cov=deepaas_full --cov-report=html tests

[testenv:qc.sec]
commands = bandit -r deepaas_full api deepaas_full -x tests

[testenv:qc.sty]
commands = flake8 --output-file=flake8.log \
   --format=pylint deepaas_full api flows tests
